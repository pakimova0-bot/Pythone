# Pythone
1
100 %
# Документация проекта

## Описание базового синтаксиса записи и форматирования

Проект использует следующие технологии и синтаксис:

1. **Python**: Основной язык программирования для написания тестов.
2. **Selenium**: Библиотека для автоматизации взаимодействия с веб-браузером.
3. **Pytest**: Фреймворк для написания и запуска тестов.
4. **Allure**: Инструмент для генерации отчетов о выполнении тестов.

### Форматирование кода

- Код форматируется в соответствии с PEP 8 (стиль написания кода на Python).
- Используются docstrings для документирования методов и функций.
- Все шаги теста размечаются с помощью `@allure.step` или `with allure.step` для улучшения читаемости отчетов.

---

## Инструкция по запуску тестов для формирования отчета Allure

1. Установите необходимые зависимости:
   ```
  pip install allure-pytest
   ```

2. Запустите тесты с генерацией отчета Allure:
   ```
   pytest --alluredir=./allure-results
   При необходимости для запуска результатов тестов нужно указать пусть а папке, куда сохранен Allure.
   Пример C:\Users\Полина\Desktop\allure-2.41.0\bin\allure.bat. Тогда команда для запуска будем иметь следующий пример
   C:\Users\Полина\Desktop\allure-2.41.0\bin\allure.bat serve allure-result.

   ```
   Эта команда запустит все тесты и сохранит результаты в директорию `./allure-results`.

---

## Инструкция по просмотру сформированного отчета Allure

1. После завершения тестов, чтобы просмотреть отчет, выполните команду:
   ```
   allure serve ./allure-results
   ```

   Эта команда запустит локальный сервер и откроет отчет в браузере.

2. В отчете Allure вы увидите:
   - Две папки с тестами lesson_10.shop; lesson_10.Calculator. Перейдем в папку lesson_10.shop.
   - **test_stor_page.py**: `Тестирование покупки в магазине Swag Labs: Сравнение итоговой суммы заказа с ожидаемой стоимостью`.
   - **Описание теста**: тест проверяет полный процесс оформления заказа в интернет-магазине.
   - **Шаги теста**:
     - Авторизация стандартного пользователя
     - Добавление товаров в корзину
     - Оформление заказа с вводом персональных данных
     - Проверка корректности итоговой суммы
     - Ожидаемый результат: Итоговая сумма должна быть равна $58.29.
     - **Результат**: Broken, Информация о проведении теста недоступна. 
     В обзоре теста мы видим ошибку _pytest.fixtures.FixtureLookupError: ('driver', <FixtureRequest for <Function test_form_submission_flow>>). 
     При переходе во складку Retries, можно ознакомится с причиной провала теста.  failed21:35:24 at 5/13/2026
     AssertionError: assert 58 == 58.29